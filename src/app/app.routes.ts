import { Routes } from '@angular/router';
import { ListComponent } from '@app/modules/to-do-list/pages/list/list.component';

export const routes: Routes = [
  {
    path: '',
    component: ListComponent,
  }
];
